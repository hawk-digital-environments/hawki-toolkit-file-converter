services:
  file-converter-test:
    build:
      context: .
      dockerfile: Dockerfile
      target: test
    entrypoint: []
    environment:
      - F_API_KEY=test-secret-key
      - POSTGRES_ENABLED=false
      - PREFECT_API_DATABASE_CONNECTION_URL=sqlite+aiosqlite:////tmp/prefect.db?timeout=60
